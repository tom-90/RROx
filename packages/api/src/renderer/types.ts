export enum RendererMode {
    WINDOW = "WINDOW",
    OVERLAY = "OVERLAY",
    WEB = "WEB"
}

export enum OverlayMode {
    HIDDEN = 0,
    SHOWN = 1,
    FOCUSSED = 2,
}

export enum ShareMode {
    NONE  ,
    HOST  ,
    CLIENT,
}