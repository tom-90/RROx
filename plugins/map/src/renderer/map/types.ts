export enum MapMode {
    // Normal mode, when showing map in desktop app
    NORMAL = 'normal',

    // Large map in-game after pressing F1
    MAP = 'map',

    // In-game minimap
    MINIMAP = 'minimap',
}